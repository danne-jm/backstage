apiVersion: v1
kind: Service
metadata:
  name: backstage-service
  namespace: default
spec:
  selector:
    app: backstage
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
  type: NodePort
